77a78,86
>     , [$.type_assertion, $.call_expression]  // <Type>value 类型断言与泛型调用 <T>() 的歧义
>     , [$.type_assertion, $.binary_expression]  // <Type>value 中 < 既是比较运算符也是类型参数开始
>     , [$.type_assertion, $.expression]  // type_assertion 与 expression 的歧义
>     , [$.type_assertion, $.binary_expression, $.call_expression]  // 三方歧义：<Type>expr vs comparison vs generic call
>     , [$.type_assertion, $.call_expression, $.member_expression, $.subscript_expression]  // 四方歧义
>     , [$.object_type, $.block_statement]  // { x: number } 类型 vs 块语句歧义
>     , [$.object_type, $.object_literal]  // { x: number } 类型 vs 对象字面量歧义
>     , [$.type_member, $.property_name]  // 类型成员 vs 属性名歧义
>     , [$.block_statement, $.object_type, $.object_literal]  // {} 三方歧义
299c308
<       $.identifier,
---
>       choice($.identifier, $.private_field_identifier),  // 支持 #x 私有字段
352c361
<       seq('Button', '(', optional(choice($.expression, $.component_parameters)), ')', optional($.container_content_body)),  // Button 也可以有子组件
---
>       seq('Button', '(', optional(seq($.expression, optional(seq(',', $.component_parameters)))), ')', optional($.container_content_body)),  // Button 支持 1-2 个参数
453a463
>     private_field_identifier: $ => /#[a-zA-Z_$][a-zA-Z0-9_$]*/,  // JS private fields (#x)
494c504,505
<       $.non_null_assertion_expression  // 非空断言表达式 (value!)
---
>       $.non_null_assertion_expression,  // 非空断言表达式 (value!)
>       $.type_assertion                  // 类型断言 (<Type>value)
572a584,589
>     // 类型断言 (<Type>value) — 旧式 TS 类型断言，ArkTS 中仍有使用
>     type_assertion: $ => prec.left(2, seq(
>       $.type_arguments,
>       $.expression
>     )),
> 
605a623
>       $.object_type,     // 对象类型，如 { x: number, y: string }
651c669,672
<         $.identifier
---
>         $.identifier,
>         $.generic_type,
>         $.object_type,
>         $.parenthesized_type
756a778
>       optional(choice('public', 'private', 'protected', 'readonly')),  // 构造函数参数修饰符
947c969
<         $.identifier
---
>         choice($.identifier, $.private_field_identifier)
953c975
<         $.identifier
---
>         choice($.identifier, $.private_field_identifier)
